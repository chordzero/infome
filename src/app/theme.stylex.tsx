import stylex from "@stylexjs/stylex";

export const colors = stylex.defineVars({
  accent: "#55f",
  primaryText: "#dedede",
  secondaryText: "#ccc",
  background: "#444",
});

export const fonts = stylex.defineVars({
  primaryText: "Pretendard Variable",
})